name: 🕵️ Diagnóstico Copa (Manual)

on:
  workflow_dispatch:

jobs:
  debug_job:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Baixar Código
        uses: actions/checkout@v3

      - name: 🐍 Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      - name: 📦 Instalar Dependências
        # AQUI ESTAVA O ERRO: Agora instalamos tudo do requirements.txt
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: 🚀 Rodar Diagnóstico
        env:
          CARTOLA_GLBID: ${{ secrets.CARTOLA_GLBID }} 
        run: python debug_coletor.py
